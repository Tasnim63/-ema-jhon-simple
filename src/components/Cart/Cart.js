import React from 'react';

const Cart = ({ cart }) => {
    return (
        <div>
            <h6>order summary</h6>
            {/* <p>selected items:{cart.length}</p> */}
        </div>
    );
};

export default Cart;